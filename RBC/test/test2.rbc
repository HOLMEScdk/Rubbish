// test fun return and its correct
int fib(int n) {
	if (n == 0) 
		return 0;
	if (n == 1)
		return 1;
     return fib(n - 1) + fib(n - 2);
}

int main(void) {
	int ans;
	ans = fib(10);
	iprint(ans);
	return 0;
}